# Privy app id (https://dashboard.privy.io) — enables the Connect wallet button.
# Without it the app signs with local session personas only.
VITE_PRIVY_APP_ID=

# Written automatically by `node scripts/deploy.mjs` — never commit the real one.
DEPLOYER_PRIVATE_KEY=
